# Expense-tracker
A web application to record and manage expenses by category. It calculates total spending, provides budget alerts, shows a visual summary and allows data export. Built using HTML, CSS and JavaScript with LocalStorage for offline use as a Progressive Web App (PWA).
